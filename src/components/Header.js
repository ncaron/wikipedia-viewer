import React from 'react';

const Header = () => {
  return (
    <header>
      <i className="fa fa-wikipedia-w" />
      <p>A simple Wikipedia search built with React & Redux.</p>
    </header>
  );
};

export default Header;
